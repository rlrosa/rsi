CONTIKI_PROYECT = rsi-main
all: $(CONTIKI_PROYECT)

CONTIKI = ../../../
CFLAGS += -DPROJECT_CONF_H=\"project-conf.h\"

include $(CONTIKI)/Makefile.include

CONTIKIDIRS += ../aux
#CONTIKI_SOURCEFILES += aux.c

